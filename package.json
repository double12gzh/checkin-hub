{
  "name": "checkin-hub",
  "version": "2.0.0",
  "description": "Universal Daily Check-in Framework with Plugin Architecture",
  "main": "index.js",
  "bin": {
    "checkin-hub": "./bin/checkin-hub.js"
  },
  "scripts": {
    "start": "node index.js",
    "checkin": "node index.js",
    "checkin:all": "node index.js --all",
    "setup": "node index.js --setup agentrouter",
    "list": "node index.js --list",
    "mcp": "node mcp-server.js",
    "setup:agent": "node scripts/install-agent-tools.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "node --test 'test/**/*.test.js'",
    "check": "npm run lint && npm run format:check && npm test && node scripts/verify-plugins.js",
    "prepare": "husky"
  },
  "keywords": [
    "checkin",
    "automation",
    "playwright",
    "agentrouter"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "playwright": "^1.62.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "globals": "^17.12.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.4.1",
    "omp-dashboard": "^0.1.0",
    "prettier": "^3.9.6"
  },
  "omp": {
    "extensions": [
      "./node_modules/omp-dashboard/dist/extension/index.js"
    ]
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  }
}
